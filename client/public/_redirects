# Respaldo si netlify.toml no se aplica — API antes que SPA
/api/*  /.netlify/functions/api  200!

# Clips de voz GPS: no reescribir a index.html
/voice/*  /voice/:splat  200

/*      /index.html               200
