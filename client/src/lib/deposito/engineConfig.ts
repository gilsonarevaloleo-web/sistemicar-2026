/**
 * Reexport del Motor Depósito v2 — Universidad de Sistemicar.
 * Spec pedía `src/lib/deposito/engineConfig.ts`; el núcleo vive en
 * `@shared/deposito/engineConfig` para uso dual client/server
 * (mismo patrón que Umbral v2).
 */
export * from "@shared/deposito/engineConfig";
