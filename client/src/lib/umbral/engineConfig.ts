/**
 * Reexport del Motor Umbral v2.
 * Spec pedía `src/lib/umbral/engineConfig.ts`; el núcleo vive en `@shared/umbral/engineConfig`
 * para uso dual client/server (mismo patrón que Espejo V2).
 */
export * from "@shared/umbral/engineConfig";
